// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/usezoracle/rails-sui/ent/lpaccount"
	"github.com/usezoracle/rails-sui/ent/lpledgerentry"
	"github.com/usezoracle/rails-sui/ent/predicate"
	"github.com/usezoracle/rails-sui/ent/user"
)

// LpAccountQuery is the builder for querying LpAccount entities.
type LpAccountQuery struct {
	config
	ctx               *QueryContext
	order             []lpaccount.OrderOption
	inters            []Interceptor
	predicates        []predicate.LpAccount
	withUser          *UserQuery
	withLedgerEntries *LpLedgerEntryQuery
	withFKs           bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LpAccountQuery builder.
func (laq *LpAccountQuery) Where(ps ...predicate.LpAccount) *LpAccountQuery {
	laq.predicates = append(laq.predicates, ps...)
	return laq
}

// Limit the number of records to be returned by this query.
func (laq *LpAccountQuery) Limit(limit int) *LpAccountQuery {
	laq.ctx.Limit = &limit
	return laq
}

// Offset to start from.
func (laq *LpAccountQuery) Offset(offset int) *LpAccountQuery {
	laq.ctx.Offset = &offset
	return laq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (laq *LpAccountQuery) Unique(unique bool) *LpAccountQuery {
	laq.ctx.Unique = &unique
	return laq
}

// Order specifies how the records should be ordered.
func (laq *LpAccountQuery) Order(o ...lpaccount.OrderOption) *LpAccountQuery {
	laq.order = append(laq.order, o...)
	return laq
}

// QueryUser chains the current query on the "user" edge.
func (laq *LpAccountQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: laq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := laq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := laq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lpaccount.Table, lpaccount.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, lpaccount.UserTable, lpaccount.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(laq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLedgerEntries chains the current query on the "ledger_entries" edge.
func (laq *LpAccountQuery) QueryLedgerEntries() *LpLedgerEntryQuery {
	query := (&LpLedgerEntryClient{config: laq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := laq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := laq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lpaccount.Table, lpaccount.FieldID, selector),
			sqlgraph.To(lpledgerentry.Table, lpledgerentry.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lpaccount.LedgerEntriesTable, lpaccount.LedgerEntriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(laq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LpAccount entity from the query.
// Returns a *NotFoundError when no LpAccount was found.
func (laq *LpAccountQuery) First(ctx context.Context) (*LpAccount, error) {
	nodes, err := laq.Limit(1).All(setContextOp(ctx, laq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lpaccount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (laq *LpAccountQuery) FirstX(ctx context.Context) *LpAccount {
	node, err := laq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LpAccount ID from the query.
// Returns a *NotFoundError when no LpAccount ID was found.
func (laq *LpAccountQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = laq.Limit(1).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lpaccount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (laq *LpAccountQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := laq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LpAccount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LpAccount entity is found.
// Returns a *NotFoundError when no LpAccount entities are found.
func (laq *LpAccountQuery) Only(ctx context.Context) (*LpAccount, error) {
	nodes, err := laq.Limit(2).All(setContextOp(ctx, laq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lpaccount.Label}
	default:
		return nil, &NotSingularError{lpaccount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (laq *LpAccountQuery) OnlyX(ctx context.Context) *LpAccount {
	node, err := laq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LpAccount ID in the query.
// Returns a *NotSingularError when more than one LpAccount ID is found.
// Returns a *NotFoundError when no entities are found.
func (laq *LpAccountQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = laq.Limit(2).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lpaccount.Label}
	default:
		err = &NotSingularError{lpaccount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (laq *LpAccountQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := laq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LpAccounts.
func (laq *LpAccountQuery) All(ctx context.Context) ([]*LpAccount, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryAll)
	if err := laq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LpAccount, *LpAccountQuery]()
	return withInterceptors[[]*LpAccount](ctx, laq, qr, laq.inters)
}

// AllX is like All, but panics if an error occurs.
func (laq *LpAccountQuery) AllX(ctx context.Context) []*LpAccount {
	nodes, err := laq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LpAccount IDs.
func (laq *LpAccountQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if laq.ctx.Unique == nil && laq.path != nil {
		laq.Unique(true)
	}
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryIDs)
	if err = laq.Select(lpaccount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (laq *LpAccountQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := laq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (laq *LpAccountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryCount)
	if err := laq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, laq, querierCount[*LpAccountQuery](), laq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (laq *LpAccountQuery) CountX(ctx context.Context) int {
	count, err := laq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (laq *LpAccountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryExist)
	switch _, err := laq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (laq *LpAccountQuery) ExistX(ctx context.Context) bool {
	exist, err := laq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LpAccountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (laq *LpAccountQuery) Clone() *LpAccountQuery {
	if laq == nil {
		return nil
	}
	return &LpAccountQuery{
		config:            laq.config,
		ctx:               laq.ctx.Clone(),
		order:             append([]lpaccount.OrderOption{}, laq.order...),
		inters:            append([]Interceptor{}, laq.inters...),
		predicates:        append([]predicate.LpAccount{}, laq.predicates...),
		withUser:          laq.withUser.Clone(),
		withLedgerEntries: laq.withLedgerEntries.Clone(),
		// clone intermediate query.
		sql:  laq.sql.Clone(),
		path: laq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (laq *LpAccountQuery) WithUser(opts ...func(*UserQuery)) *LpAccountQuery {
	query := (&UserClient{config: laq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	laq.withUser = query
	return laq
}

// WithLedgerEntries tells the query-builder to eager-load the nodes that are connected to
// the "ledger_entries" edge. The optional arguments are used to configure the query builder of the edge.
func (laq *LpAccountQuery) WithLedgerEntries(opts ...func(*LpLedgerEntryQuery)) *LpAccountQuery {
	query := (&LpLedgerEntryClient{config: laq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	laq.withLedgerEntries = query
	return laq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LpAccount.Query().
//		GroupBy(lpaccount.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (laq *LpAccountQuery) GroupBy(field string, fields ...string) *LpAccountGroupBy {
	laq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LpAccountGroupBy{build: laq}
	grbuild.flds = &laq.ctx.Fields
	grbuild.label = lpaccount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.LpAccount.Query().
//		Select(lpaccount.FieldCreatedAt).
//		Scan(ctx, &v)
func (laq *LpAccountQuery) Select(fields ...string) *LpAccountSelect {
	laq.ctx.Fields = append(laq.ctx.Fields, fields...)
	sbuild := &LpAccountSelect{LpAccountQuery: laq}
	sbuild.label = lpaccount.Label
	sbuild.flds, sbuild.scan = &laq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LpAccountSelect configured with the given aggregations.
func (laq *LpAccountQuery) Aggregate(fns ...AggregateFunc) *LpAccountSelect {
	return laq.Select().Aggregate(fns...)
}

func (laq *LpAccountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range laq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, laq); err != nil {
				return err
			}
		}
	}
	for _, f := range laq.ctx.Fields {
		if !lpaccount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if laq.path != nil {
		prev, err := laq.path(ctx)
		if err != nil {
			return err
		}
		laq.sql = prev
	}
	return nil
}

func (laq *LpAccountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LpAccount, error) {
	var (
		nodes       = []*LpAccount{}
		withFKs     = laq.withFKs
		_spec       = laq.querySpec()
		loadedTypes = [2]bool{
			laq.withUser != nil,
			laq.withLedgerEntries != nil,
		}
	)
	if laq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, lpaccount.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LpAccount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LpAccount{config: laq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, laq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := laq.withUser; query != nil {
		if err := laq.loadUser(ctx, query, nodes, nil,
			func(n *LpAccount, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := laq.withLedgerEntries; query != nil {
		if err := laq.loadLedgerEntries(ctx, query, nodes,
			func(n *LpAccount) { n.Edges.LedgerEntries = []*LpLedgerEntry{} },
			func(n *LpAccount, e *LpLedgerEntry) { n.Edges.LedgerEntries = append(n.Edges.LedgerEntries, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (laq *LpAccountQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LpAccount, init func(*LpAccount), assign func(*LpAccount, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LpAccount)
	for i := range nodes {
		if nodes[i].user_lp_account == nil {
			continue
		}
		fk := *nodes[i].user_lp_account
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_lp_account" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (laq *LpAccountQuery) loadLedgerEntries(ctx context.Context, query *LpLedgerEntryQuery, nodes []*LpAccount, init func(*LpAccount), assign func(*LpAccount, *LpLedgerEntry)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*LpAccount)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.LpLedgerEntry(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lpaccount.LedgerEntriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.lp_account_ledger_entries
		if fk == nil {
			return fmt.Errorf(`foreign-key "lp_account_ledger_entries" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "lp_account_ledger_entries" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (laq *LpAccountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := laq.querySpec()
	_spec.Node.Columns = laq.ctx.Fields
	if len(laq.ctx.Fields) > 0 {
		_spec.Unique = laq.ctx.Unique != nil && *laq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, laq.driver, _spec)
}

func (laq *LpAccountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lpaccount.Table, lpaccount.Columns, sqlgraph.NewFieldSpec(lpaccount.FieldID, field.TypeUUID))
	_spec.From = laq.sql
	if unique := laq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if laq.path != nil {
		_spec.Unique = true
	}
	if fields := laq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lpaccount.FieldID)
		for i := range fields {
			if fields[i] != lpaccount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := laq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := laq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := laq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := laq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (laq *LpAccountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(laq.driver.Dialect())
	t1 := builder.Table(lpaccount.Table)
	columns := laq.ctx.Fields
	if len(columns) == 0 {
		columns = lpaccount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if laq.sql != nil {
		selector = laq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if laq.ctx.Unique != nil && *laq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range laq.predicates {
		p(selector)
	}
	for _, p := range laq.order {
		p(selector)
	}
	if offset := laq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := laq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LpAccountGroupBy is the group-by builder for LpAccount entities.
type LpAccountGroupBy struct {
	selector
	build *LpAccountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lagb *LpAccountGroupBy) Aggregate(fns ...AggregateFunc) *LpAccountGroupBy {
	lagb.fns = append(lagb.fns, fns...)
	return lagb
}

// Scan applies the selector query and scans the result into the given value.
func (lagb *LpAccountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lagb.build.ctx, ent.OpQueryGroupBy)
	if err := lagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LpAccountQuery, *LpAccountGroupBy](ctx, lagb.build, lagb, lagb.build.inters, v)
}

func (lagb *LpAccountGroupBy) sqlScan(ctx context.Context, root *LpAccountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lagb.fns))
	for _, fn := range lagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lagb.flds)+len(lagb.fns))
		for _, f := range *lagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LpAccountSelect is the builder for selecting fields of LpAccount entities.
type LpAccountSelect struct {
	*LpAccountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (las *LpAccountSelect) Aggregate(fns ...AggregateFunc) *LpAccountSelect {
	las.fns = append(las.fns, fns...)
	return las
}

// Scan applies the selector query and scans the result into the given value.
func (las *LpAccountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, las.ctx, ent.OpQuerySelect)
	if err := las.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LpAccountQuery, *LpAccountSelect](ctx, las.LpAccountQuery, las, las.inters, v)
}

func (las *LpAccountSelect) sqlScan(ctx context.Context, root *LpAccountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(las.fns))
	for _, fn := range las.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*las.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := las.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
