// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/usezoracle/rails-sui/ent/tappcard"
	"github.com/usezoracle/rails-sui/ent/user"
)

// TappCard is the model entity for the TappCard schema.
type TappCard struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// ActivationToken holds the value of the "activation_token" field.
	ActivationToken string `json:"activation_token,omitempty"`
	// Status holds the value of the "status" field.
	Status tappcard.Status `json:"status,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TappCardQuery when eager-loading is set.
	Edges           TappCardEdges `json:"edges"`
	user_tapp_cards *uuid.UUID
	selectValues    sql.SelectValues
}

// TappCardEdges holds the relations/edges for other nodes in the graph.
type TappCardEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TappCardEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TappCard) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tappcard.FieldActivationToken, tappcard.FieldStatus:
			values[i] = new(sql.NullString)
		case tappcard.FieldCreatedAt, tappcard.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case tappcard.FieldID:
			values[i] = new(uuid.UUID)
		case tappcard.ForeignKeys[0]: // user_tapp_cards
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TappCard fields.
func (tc *TappCard) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tappcard.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tc.ID = *value
			}
		case tappcard.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tc.CreatedAt = value.Time
			}
		case tappcard.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tc.UpdatedAt = value.Time
			}
		case tappcard.FieldActivationToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field activation_token", values[i])
			} else if value.Valid {
				tc.ActivationToken = value.String
			}
		case tappcard.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				tc.Status = tappcard.Status(value.String)
			}
		case tappcard.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_tapp_cards", values[i])
			} else if value.Valid {
				tc.user_tapp_cards = new(uuid.UUID)
				*tc.user_tapp_cards = *value.S.(*uuid.UUID)
			}
		default:
			tc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TappCard.
// This includes values selected through modifiers, order, etc.
func (tc *TappCard) Value(name string) (ent.Value, error) {
	return tc.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the TappCard entity.
func (tc *TappCard) QueryUser() *UserQuery {
	return NewTappCardClient(tc.config).QueryUser(tc)
}

// Update returns a builder for updating this TappCard.
// Note that you need to call TappCard.Unwrap() before calling this method if this TappCard
// was returned from a transaction, and the transaction was committed or rolled back.
func (tc *TappCard) Update() *TappCardUpdateOne {
	return NewTappCardClient(tc.config).UpdateOne(tc)
}

// Unwrap unwraps the TappCard entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tc *TappCard) Unwrap() *TappCard {
	_tx, ok := tc.config.driver.(*txDriver)
	if !ok {
		panic("ent: TappCard is not a transactional entity")
	}
	tc.config.driver = _tx.drv
	return tc
}

// String implements the fmt.Stringer.
func (tc *TappCard) String() string {
	var builder strings.Builder
	builder.WriteString("TappCard(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tc.ID))
	builder.WriteString("created_at=")
	builder.WriteString(tc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(tc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("activation_token=")
	builder.WriteString(tc.ActivationToken)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", tc.Status))
	builder.WriteByte(')')
	return builder.String()
}

// TappCards is a parsable slice of TappCard.
type TappCards []*TappCard
