// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/usezoracle/rails-sui/ent/cardservernonce"
	"github.com/usezoracle/rails-sui/ent/predicate"
	"github.com/usezoracle/rails-sui/ent/senderprofile"
	"github.com/usezoracle/rails-sui/ent/tappcard"
)

// CardServerNonceQuery is the builder for querying CardServerNonce entities.
type CardServerNonceQuery struct {
	config
	ctx               *QueryContext
	order             []cardservernonce.OrderOption
	inters            []Interceptor
	predicates        []predicate.CardServerNonce
	withCard          *TappCardQuery
	withSenderProfile *SenderProfileQuery
	withFKs           bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CardServerNonceQuery builder.
func (csnq *CardServerNonceQuery) Where(ps ...predicate.CardServerNonce) *CardServerNonceQuery {
	csnq.predicates = append(csnq.predicates, ps...)
	return csnq
}

// Limit the number of records to be returned by this query.
func (csnq *CardServerNonceQuery) Limit(limit int) *CardServerNonceQuery {
	csnq.ctx.Limit = &limit
	return csnq
}

// Offset to start from.
func (csnq *CardServerNonceQuery) Offset(offset int) *CardServerNonceQuery {
	csnq.ctx.Offset = &offset
	return csnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csnq *CardServerNonceQuery) Unique(unique bool) *CardServerNonceQuery {
	csnq.ctx.Unique = &unique
	return csnq
}

// Order specifies how the records should be ordered.
func (csnq *CardServerNonceQuery) Order(o ...cardservernonce.OrderOption) *CardServerNonceQuery {
	csnq.order = append(csnq.order, o...)
	return csnq
}

// QueryCard chains the current query on the "card" edge.
func (csnq *CardServerNonceQuery) QueryCard() *TappCardQuery {
	query := (&TappCardClient{config: csnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cardservernonce.Table, cardservernonce.FieldID, selector),
			sqlgraph.To(tappcard.Table, tappcard.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cardservernonce.CardTable, cardservernonce.CardColumn),
		)
		fromU = sqlgraph.SetNeighbors(csnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySenderProfile chains the current query on the "sender_profile" edge.
func (csnq *CardServerNonceQuery) QuerySenderProfile() *SenderProfileQuery {
	query := (&SenderProfileClient{config: csnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cardservernonce.Table, cardservernonce.FieldID, selector),
			sqlgraph.To(senderprofile.Table, senderprofile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cardservernonce.SenderProfileTable, cardservernonce.SenderProfileColumn),
		)
		fromU = sqlgraph.SetNeighbors(csnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CardServerNonce entity from the query.
// Returns a *NotFoundError when no CardServerNonce was found.
func (csnq *CardServerNonceQuery) First(ctx context.Context) (*CardServerNonce, error) {
	nodes, err := csnq.Limit(1).All(setContextOp(ctx, csnq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cardservernonce.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csnq *CardServerNonceQuery) FirstX(ctx context.Context) *CardServerNonce {
	node, err := csnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CardServerNonce ID from the query.
// Returns a *NotFoundError when no CardServerNonce ID was found.
func (csnq *CardServerNonceQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csnq.Limit(1).IDs(setContextOp(ctx, csnq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cardservernonce.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csnq *CardServerNonceQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := csnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CardServerNonce entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CardServerNonce entity is found.
// Returns a *NotFoundError when no CardServerNonce entities are found.
func (csnq *CardServerNonceQuery) Only(ctx context.Context) (*CardServerNonce, error) {
	nodes, err := csnq.Limit(2).All(setContextOp(ctx, csnq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cardservernonce.Label}
	default:
		return nil, &NotSingularError{cardservernonce.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csnq *CardServerNonceQuery) OnlyX(ctx context.Context) *CardServerNonce {
	node, err := csnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CardServerNonce ID in the query.
// Returns a *NotSingularError when more than one CardServerNonce ID is found.
// Returns a *NotFoundError when no entities are found.
func (csnq *CardServerNonceQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csnq.Limit(2).IDs(setContextOp(ctx, csnq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cardservernonce.Label}
	default:
		err = &NotSingularError{cardservernonce.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csnq *CardServerNonceQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := csnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CardServerNonces.
func (csnq *CardServerNonceQuery) All(ctx context.Context) ([]*CardServerNonce, error) {
	ctx = setContextOp(ctx, csnq.ctx, ent.OpQueryAll)
	if err := csnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CardServerNonce, *CardServerNonceQuery]()
	return withInterceptors[[]*CardServerNonce](ctx, csnq, qr, csnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csnq *CardServerNonceQuery) AllX(ctx context.Context) []*CardServerNonce {
	nodes, err := csnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CardServerNonce IDs.
func (csnq *CardServerNonceQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if csnq.ctx.Unique == nil && csnq.path != nil {
		csnq.Unique(true)
	}
	ctx = setContextOp(ctx, csnq.ctx, ent.OpQueryIDs)
	if err = csnq.Select(cardservernonce.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csnq *CardServerNonceQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := csnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csnq *CardServerNonceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csnq.ctx, ent.OpQueryCount)
	if err := csnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csnq, querierCount[*CardServerNonceQuery](), csnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csnq *CardServerNonceQuery) CountX(ctx context.Context) int {
	count, err := csnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csnq *CardServerNonceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csnq.ctx, ent.OpQueryExist)
	switch _, err := csnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csnq *CardServerNonceQuery) ExistX(ctx context.Context) bool {
	exist, err := csnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CardServerNonceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csnq *CardServerNonceQuery) Clone() *CardServerNonceQuery {
	if csnq == nil {
		return nil
	}
	return &CardServerNonceQuery{
		config:            csnq.config,
		ctx:               csnq.ctx.Clone(),
		order:             append([]cardservernonce.OrderOption{}, csnq.order...),
		inters:            append([]Interceptor{}, csnq.inters...),
		predicates:        append([]predicate.CardServerNonce{}, csnq.predicates...),
		withCard:          csnq.withCard.Clone(),
		withSenderProfile: csnq.withSenderProfile.Clone(),
		// clone intermediate query.
		sql:  csnq.sql.Clone(),
		path: csnq.path,
	}
}

// WithCard tells the query-builder to eager-load the nodes that are connected to
// the "card" edge. The optional arguments are used to configure the query builder of the edge.
func (csnq *CardServerNonceQuery) WithCard(opts ...func(*TappCardQuery)) *CardServerNonceQuery {
	query := (&TappCardClient{config: csnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csnq.withCard = query
	return csnq
}

// WithSenderProfile tells the query-builder to eager-load the nodes that are connected to
// the "sender_profile" edge. The optional arguments are used to configure the query builder of the edge.
func (csnq *CardServerNonceQuery) WithSenderProfile(opts ...func(*SenderProfileQuery)) *CardServerNonceQuery {
	query := (&SenderProfileClient{config: csnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csnq.withSenderProfile = query
	return csnq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CardServerNonce.Query().
//		GroupBy(cardservernonce.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csnq *CardServerNonceQuery) GroupBy(field string, fields ...string) *CardServerNonceGroupBy {
	csnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CardServerNonceGroupBy{build: csnq}
	grbuild.flds = &csnq.ctx.Fields
	grbuild.label = cardservernonce.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CardServerNonce.Query().
//		Select(cardservernonce.FieldCreatedAt).
//		Scan(ctx, &v)
func (csnq *CardServerNonceQuery) Select(fields ...string) *CardServerNonceSelect {
	csnq.ctx.Fields = append(csnq.ctx.Fields, fields...)
	sbuild := &CardServerNonceSelect{CardServerNonceQuery: csnq}
	sbuild.label = cardservernonce.Label
	sbuild.flds, sbuild.scan = &csnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CardServerNonceSelect configured with the given aggregations.
func (csnq *CardServerNonceQuery) Aggregate(fns ...AggregateFunc) *CardServerNonceSelect {
	return csnq.Select().Aggregate(fns...)
}

func (csnq *CardServerNonceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csnq); err != nil {
				return err
			}
		}
	}
	for _, f := range csnq.ctx.Fields {
		if !cardservernonce.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csnq.path != nil {
		prev, err := csnq.path(ctx)
		if err != nil {
			return err
		}
		csnq.sql = prev
	}
	return nil
}

func (csnq *CardServerNonceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CardServerNonce, error) {
	var (
		nodes       = []*CardServerNonce{}
		withFKs     = csnq.withFKs
		_spec       = csnq.querySpec()
		loadedTypes = [2]bool{
			csnq.withCard != nil,
			csnq.withSenderProfile != nil,
		}
	)
	if csnq.withCard != nil || csnq.withSenderProfile != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, cardservernonce.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CardServerNonce).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CardServerNonce{config: csnq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csnq.withCard; query != nil {
		if err := csnq.loadCard(ctx, query, nodes, nil,
			func(n *CardServerNonce, e *TappCard) { n.Edges.Card = e }); err != nil {
			return nil, err
		}
	}
	if query := csnq.withSenderProfile; query != nil {
		if err := csnq.loadSenderProfile(ctx, query, nodes, nil,
			func(n *CardServerNonce, e *SenderProfile) { n.Edges.SenderProfile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csnq *CardServerNonceQuery) loadCard(ctx context.Context, query *TappCardQuery, nodes []*CardServerNonce, init func(*CardServerNonce), assign func(*CardServerNonce, *TappCard)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CardServerNonce)
	for i := range nodes {
		if nodes[i].tapp_card_server_nonces == nil {
			continue
		}
		fk := *nodes[i].tapp_card_server_nonces
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tappcard.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tapp_card_server_nonces" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csnq *CardServerNonceQuery) loadSenderProfile(ctx context.Context, query *SenderProfileQuery, nodes []*CardServerNonce, init func(*CardServerNonce), assign func(*CardServerNonce, *SenderProfile)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CardServerNonce)
	for i := range nodes {
		if nodes[i].sender_profile_card_server_nonces == nil {
			continue
		}
		fk := *nodes[i].sender_profile_card_server_nonces
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(senderprofile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "sender_profile_card_server_nonces" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (csnq *CardServerNonceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csnq.querySpec()
	_spec.Node.Columns = csnq.ctx.Fields
	if len(csnq.ctx.Fields) > 0 {
		_spec.Unique = csnq.ctx.Unique != nil && *csnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csnq.driver, _spec)
}

func (csnq *CardServerNonceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cardservernonce.Table, cardservernonce.Columns, sqlgraph.NewFieldSpec(cardservernonce.FieldID, field.TypeUUID))
	_spec.From = csnq.sql
	if unique := csnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csnq.path != nil {
		_spec.Unique = true
	}
	if fields := csnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cardservernonce.FieldID)
		for i := range fields {
			if fields[i] != cardservernonce.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csnq *CardServerNonceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csnq.driver.Dialect())
	t1 := builder.Table(cardservernonce.Table)
	columns := csnq.ctx.Fields
	if len(columns) == 0 {
		columns = cardservernonce.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csnq.sql != nil {
		selector = csnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csnq.ctx.Unique != nil && *csnq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csnq.predicates {
		p(selector)
	}
	for _, p := range csnq.order {
		p(selector)
	}
	if offset := csnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CardServerNonceGroupBy is the group-by builder for CardServerNonce entities.
type CardServerNonceGroupBy struct {
	selector
	build *CardServerNonceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csngb *CardServerNonceGroupBy) Aggregate(fns ...AggregateFunc) *CardServerNonceGroupBy {
	csngb.fns = append(csngb.fns, fns...)
	return csngb
}

// Scan applies the selector query and scans the result into the given value.
func (csngb *CardServerNonceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csngb.build.ctx, ent.OpQueryGroupBy)
	if err := csngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CardServerNonceQuery, *CardServerNonceGroupBy](ctx, csngb.build, csngb, csngb.build.inters, v)
}

func (csngb *CardServerNonceGroupBy) sqlScan(ctx context.Context, root *CardServerNonceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csngb.fns))
	for _, fn := range csngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csngb.flds)+len(csngb.fns))
		for _, f := range *csngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CardServerNonceSelect is the builder for selecting fields of CardServerNonce entities.
type CardServerNonceSelect struct {
	*CardServerNonceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (csns *CardServerNonceSelect) Aggregate(fns ...AggregateFunc) *CardServerNonceSelect {
	csns.fns = append(csns.fns, fns...)
	return csns
}

// Scan applies the selector query and scans the result into the given value.
func (csns *CardServerNonceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csns.ctx, ent.OpQuerySelect)
	if err := csns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CardServerNonceQuery, *CardServerNonceSelect](ctx, csns.CardServerNonceQuery, csns, csns.inters, v)
}

func (csns *CardServerNonceSelect) sqlScan(ctx context.Context, root *CardServerNonceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(csns.fns))
	for _, fn := range csns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*csns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
