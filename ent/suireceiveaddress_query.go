// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/usezoracle/rails-sui/ent/paymentorder"
	"github.com/usezoracle/rails-sui/ent/predicate"
	"github.com/usezoracle/rails-sui/ent/suireceiveaddress"
)

// SuiReceiveAddressQuery is the builder for querying SuiReceiveAddress entities.
type SuiReceiveAddressQuery struct {
	config
	ctx              *QueryContext
	order            []suireceiveaddress.OrderOption
	inters           []Interceptor
	predicates       []predicate.SuiReceiveAddress
	withPaymentOrder *PaymentOrderQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SuiReceiveAddressQuery builder.
func (sraq *SuiReceiveAddressQuery) Where(ps ...predicate.SuiReceiveAddress) *SuiReceiveAddressQuery {
	sraq.predicates = append(sraq.predicates, ps...)
	return sraq
}

// Limit the number of records to be returned by this query.
func (sraq *SuiReceiveAddressQuery) Limit(limit int) *SuiReceiveAddressQuery {
	sraq.ctx.Limit = &limit
	return sraq
}

// Offset to start from.
func (sraq *SuiReceiveAddressQuery) Offset(offset int) *SuiReceiveAddressQuery {
	sraq.ctx.Offset = &offset
	return sraq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sraq *SuiReceiveAddressQuery) Unique(unique bool) *SuiReceiveAddressQuery {
	sraq.ctx.Unique = &unique
	return sraq
}

// Order specifies how the records should be ordered.
func (sraq *SuiReceiveAddressQuery) Order(o ...suireceiveaddress.OrderOption) *SuiReceiveAddressQuery {
	sraq.order = append(sraq.order, o...)
	return sraq
}

// QueryPaymentOrder chains the current query on the "payment_order" edge.
func (sraq *SuiReceiveAddressQuery) QueryPaymentOrder() *PaymentOrderQuery {
	query := (&PaymentOrderClient{config: sraq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sraq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sraq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(suireceiveaddress.Table, suireceiveaddress.FieldID, selector),
			sqlgraph.To(paymentorder.Table, paymentorder.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, suireceiveaddress.PaymentOrderTable, suireceiveaddress.PaymentOrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(sraq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SuiReceiveAddress entity from the query.
// Returns a *NotFoundError when no SuiReceiveAddress was found.
func (sraq *SuiReceiveAddressQuery) First(ctx context.Context) (*SuiReceiveAddress, error) {
	nodes, err := sraq.Limit(1).All(setContextOp(ctx, sraq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{suireceiveaddress.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) FirstX(ctx context.Context) *SuiReceiveAddress {
	node, err := sraq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SuiReceiveAddress ID from the query.
// Returns a *NotFoundError when no SuiReceiveAddress ID was found.
func (sraq *SuiReceiveAddressQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sraq.Limit(1).IDs(setContextOp(ctx, sraq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{suireceiveaddress.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) FirstIDX(ctx context.Context) int {
	id, err := sraq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SuiReceiveAddress entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SuiReceiveAddress entity is found.
// Returns a *NotFoundError when no SuiReceiveAddress entities are found.
func (sraq *SuiReceiveAddressQuery) Only(ctx context.Context) (*SuiReceiveAddress, error) {
	nodes, err := sraq.Limit(2).All(setContextOp(ctx, sraq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{suireceiveaddress.Label}
	default:
		return nil, &NotSingularError{suireceiveaddress.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) OnlyX(ctx context.Context) *SuiReceiveAddress {
	node, err := sraq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SuiReceiveAddress ID in the query.
// Returns a *NotSingularError when more than one SuiReceiveAddress ID is found.
// Returns a *NotFoundError when no entities are found.
func (sraq *SuiReceiveAddressQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sraq.Limit(2).IDs(setContextOp(ctx, sraq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{suireceiveaddress.Label}
	default:
		err = &NotSingularError{suireceiveaddress.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) OnlyIDX(ctx context.Context) int {
	id, err := sraq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SuiReceiveAddresses.
func (sraq *SuiReceiveAddressQuery) All(ctx context.Context) ([]*SuiReceiveAddress, error) {
	ctx = setContextOp(ctx, sraq.ctx, ent.OpQueryAll)
	if err := sraq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SuiReceiveAddress, *SuiReceiveAddressQuery]()
	return withInterceptors[[]*SuiReceiveAddress](ctx, sraq, qr, sraq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) AllX(ctx context.Context) []*SuiReceiveAddress {
	nodes, err := sraq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SuiReceiveAddress IDs.
func (sraq *SuiReceiveAddressQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sraq.ctx.Unique == nil && sraq.path != nil {
		sraq.Unique(true)
	}
	ctx = setContextOp(ctx, sraq.ctx, ent.OpQueryIDs)
	if err = sraq.Select(suireceiveaddress.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) IDsX(ctx context.Context) []int {
	ids, err := sraq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sraq *SuiReceiveAddressQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sraq.ctx, ent.OpQueryCount)
	if err := sraq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sraq, querierCount[*SuiReceiveAddressQuery](), sraq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) CountX(ctx context.Context) int {
	count, err := sraq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sraq *SuiReceiveAddressQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sraq.ctx, ent.OpQueryExist)
	switch _, err := sraq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sraq *SuiReceiveAddressQuery) ExistX(ctx context.Context) bool {
	exist, err := sraq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SuiReceiveAddressQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sraq *SuiReceiveAddressQuery) Clone() *SuiReceiveAddressQuery {
	if sraq == nil {
		return nil
	}
	return &SuiReceiveAddressQuery{
		config:           sraq.config,
		ctx:              sraq.ctx.Clone(),
		order:            append([]suireceiveaddress.OrderOption{}, sraq.order...),
		inters:           append([]Interceptor{}, sraq.inters...),
		predicates:       append([]predicate.SuiReceiveAddress{}, sraq.predicates...),
		withPaymentOrder: sraq.withPaymentOrder.Clone(),
		// clone intermediate query.
		sql:  sraq.sql.Clone(),
		path: sraq.path,
	}
}

// WithPaymentOrder tells the query-builder to eager-load the nodes that are connected to
// the "payment_order" edge. The optional arguments are used to configure the query builder of the edge.
func (sraq *SuiReceiveAddressQuery) WithPaymentOrder(opts ...func(*PaymentOrderQuery)) *SuiReceiveAddressQuery {
	query := (&PaymentOrderClient{config: sraq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sraq.withPaymentOrder = query
	return sraq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SuiReceiveAddress.Query().
//		GroupBy(suireceiveaddress.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sraq *SuiReceiveAddressQuery) GroupBy(field string, fields ...string) *SuiReceiveAddressGroupBy {
	sraq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SuiReceiveAddressGroupBy{build: sraq}
	grbuild.flds = &sraq.ctx.Fields
	grbuild.label = suireceiveaddress.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SuiReceiveAddress.Query().
//		Select(suireceiveaddress.FieldCreatedAt).
//		Scan(ctx, &v)
func (sraq *SuiReceiveAddressQuery) Select(fields ...string) *SuiReceiveAddressSelect {
	sraq.ctx.Fields = append(sraq.ctx.Fields, fields...)
	sbuild := &SuiReceiveAddressSelect{SuiReceiveAddressQuery: sraq}
	sbuild.label = suireceiveaddress.Label
	sbuild.flds, sbuild.scan = &sraq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SuiReceiveAddressSelect configured with the given aggregations.
func (sraq *SuiReceiveAddressQuery) Aggregate(fns ...AggregateFunc) *SuiReceiveAddressSelect {
	return sraq.Select().Aggregate(fns...)
}

func (sraq *SuiReceiveAddressQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sraq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sraq); err != nil {
				return err
			}
		}
	}
	for _, f := range sraq.ctx.Fields {
		if !suireceiveaddress.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sraq.path != nil {
		prev, err := sraq.path(ctx)
		if err != nil {
			return err
		}
		sraq.sql = prev
	}
	return nil
}

func (sraq *SuiReceiveAddressQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SuiReceiveAddress, error) {
	var (
		nodes       = []*SuiReceiveAddress{}
		withFKs     = sraq.withFKs
		_spec       = sraq.querySpec()
		loadedTypes = [1]bool{
			sraq.withPaymentOrder != nil,
		}
	)
	if sraq.withPaymentOrder != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, suireceiveaddress.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SuiReceiveAddress).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SuiReceiveAddress{config: sraq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sraq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sraq.withPaymentOrder; query != nil {
		if err := sraq.loadPaymentOrder(ctx, query, nodes, nil,
			func(n *SuiReceiveAddress, e *PaymentOrder) { n.Edges.PaymentOrder = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sraq *SuiReceiveAddressQuery) loadPaymentOrder(ctx context.Context, query *PaymentOrderQuery, nodes []*SuiReceiveAddress, init func(*SuiReceiveAddress), assign func(*SuiReceiveAddress, *PaymentOrder)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SuiReceiveAddress)
	for i := range nodes {
		if nodes[i].payment_order_sui_receive_address == nil {
			continue
		}
		fk := *nodes[i].payment_order_sui_receive_address
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(paymentorder.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "payment_order_sui_receive_address" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sraq *SuiReceiveAddressQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sraq.querySpec()
	_spec.Node.Columns = sraq.ctx.Fields
	if len(sraq.ctx.Fields) > 0 {
		_spec.Unique = sraq.ctx.Unique != nil && *sraq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sraq.driver, _spec)
}

func (sraq *SuiReceiveAddressQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(suireceiveaddress.Table, suireceiveaddress.Columns, sqlgraph.NewFieldSpec(suireceiveaddress.FieldID, field.TypeInt))
	_spec.From = sraq.sql
	if unique := sraq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sraq.path != nil {
		_spec.Unique = true
	}
	if fields := sraq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, suireceiveaddress.FieldID)
		for i := range fields {
			if fields[i] != suireceiveaddress.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sraq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sraq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sraq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sraq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sraq *SuiReceiveAddressQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sraq.driver.Dialect())
	t1 := builder.Table(suireceiveaddress.Table)
	columns := sraq.ctx.Fields
	if len(columns) == 0 {
		columns = suireceiveaddress.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sraq.sql != nil {
		selector = sraq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sraq.ctx.Unique != nil && *sraq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sraq.predicates {
		p(selector)
	}
	for _, p := range sraq.order {
		p(selector)
	}
	if offset := sraq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sraq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SuiReceiveAddressGroupBy is the group-by builder for SuiReceiveAddress entities.
type SuiReceiveAddressGroupBy struct {
	selector
	build *SuiReceiveAddressQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sragb *SuiReceiveAddressGroupBy) Aggregate(fns ...AggregateFunc) *SuiReceiveAddressGroupBy {
	sragb.fns = append(sragb.fns, fns...)
	return sragb
}

// Scan applies the selector query and scans the result into the given value.
func (sragb *SuiReceiveAddressGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sragb.build.ctx, ent.OpQueryGroupBy)
	if err := sragb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SuiReceiveAddressQuery, *SuiReceiveAddressGroupBy](ctx, sragb.build, sragb, sragb.build.inters, v)
}

func (sragb *SuiReceiveAddressGroupBy) sqlScan(ctx context.Context, root *SuiReceiveAddressQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sragb.fns))
	for _, fn := range sragb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sragb.flds)+len(sragb.fns))
		for _, f := range *sragb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sragb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sragb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SuiReceiveAddressSelect is the builder for selecting fields of SuiReceiveAddress entities.
type SuiReceiveAddressSelect struct {
	*SuiReceiveAddressQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sras *SuiReceiveAddressSelect) Aggregate(fns ...AggregateFunc) *SuiReceiveAddressSelect {
	sras.fns = append(sras.fns, fns...)
	return sras
}

// Scan applies the selector query and scans the result into the given value.
func (sras *SuiReceiveAddressSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sras.ctx, ent.OpQuerySelect)
	if err := sras.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SuiReceiveAddressQuery, *SuiReceiveAddressSelect](ctx, sras.SuiReceiveAddressQuery, sras, sras.inters, v)
}

func (sras *SuiReceiveAddressSelect) sqlScan(ctx context.Context, root *SuiReceiveAddressQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sras.fns))
	for _, fn := range sras.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sras.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sras.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
